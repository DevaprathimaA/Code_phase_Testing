name: Security Scan

on:
  push:
    branches:
      - main  # Change this to your default branch name if different

jobs:
  security_scan:
    name: Security Scan
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout the code from the repository
      - name: Checkout Code
        uses: actions/checkout@v2

      # Step 2: Install Gitleaks
      - name: Install Gitleaks
        run: |
          wget -q -O /tmp/gitleaks.tar.gz https://github.com/zricethezav/gitleaks/releases/latest/download/gitleaks-linux-amd64.tar.gz
          tar -xzvf /tmp/gitleaks.tar.gz -C /tmp/
          sudo mv /tmp/gitleaks-linux-amd64/gitleaks /usr/local/bin/

      # Step 3: Run Gitleaks
      - name: Run Gitleaks
        run: gitleaks --path=./

      # Step 4: Install Trivy
      - name: Install Trivy
        run: |
          sudo apt-get update
          sudo apt-get install -y wget apt-transport-https gnupg lsb-release
          wget -qO - https://aquasecurity.github.io/trivy-repo/deb/public.key | sudo gpg --dearmor -o /usr/share/keyrings/trivy-archive-keyring.gpg
          echo "deb [signed-by=/usr/share/keyrings/trivy-archive-keyring.gpg] https://aquasecurity.github.io/trivy-repo/deb $(lsb_release -sc) main" | sudo tee /etc/apt/sources.list.d/trivy.list
          sudo apt-get update
          sudo apt-get install -y trivy

      # Step 5: Run Trivy for container image scanning (optional)
      # Replace 'your-container-image' with the actual image name if applicable.
      - name: Run Trivy for Container Image
        run: trivy image your-container-image || true

      # Step 6: Run Trivy for file system scanning
      - name: Run Trivy for File System
        run: trivy filesystem --exit-code 1 --severity HIGH,CRITICAL ./ || true
